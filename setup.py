import setuptools

setuptools.setup(
    name="dotfiles_installer",
    version="1.0",
    description="Jayfaza's dotfiles installer",
    author="Jayfaza",
    platforms='any',
    packages=setuptools.find_packages(),
)
